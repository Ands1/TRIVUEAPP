<%= form_with(model: user_quiz_answer) do |form| %>
  <% if user_quiz_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_quiz_answer.errors.count, "error") %> prohibited this user_quiz_answer from being saved:</h2>

      <ul>
        <% user_quiz_answer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
