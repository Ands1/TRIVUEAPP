<%= form_with(model: quiz_question_answer) do |form| %>
  <% if quiz_question_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(quiz_question_answer.errors.count, "error") %> prohibited this quiz_question_answer from being saved:</h2>

      <ul>
        <% quiz_question_answer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="fields">
        <%= form.label :status, class:"input-group-text" %>
        <%= form.select(:status, Quiz::Question.statuses.keys.map {|reply| [reply.titleize,reply ]},{},{class:"form-select"})%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
